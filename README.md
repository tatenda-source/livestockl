
  # Build According to Specifications

  This is a code bundle for Build According to Specifications. The original project is available at https://www.figma.com/design/56b9mdb0w48h4XQ7tshLcI/Build-According-to-Specifications.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  